[tool.poetry]
name = "simsi_transfer"
version = "0.1.0"
description = ""
authors = ["Firas Hamood <firas.hamood@tum.de>"]

[tool.poetry.dependencies]
python = "^3.8"
numpy = "^1.18.1"
pandas = "^1.3.0"

[tool.poetry.dev-dependencies]
pytest = "^6.2.4"
pytest-cov = "^2.12.1"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
